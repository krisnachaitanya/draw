{
  "name": "http",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/bin.ts --bundle --platform=node --outdir=dist",
    "start": "node dist/bin.js",
    "dev": "npm run build && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "@repo/config": "workspace:*",
    "@repo/db": "workspace:*"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@repo/common": "workspace:*",
    "@types/jsonwebtoken": "^9.0.8",
    "esbuild": "^0.24.2"
  }
}
